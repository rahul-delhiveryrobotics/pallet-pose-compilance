# Runtime + dev dependencies for pallet-pose-compliance.
# Pinned to compatible-release ranges for reproducibility (R28).
numpy>=1.26,<3.0
opencv-python>=4.8,<5.0
pydantic>=2.5,<3.0
PyYAML>=6.0,<7.0
ultralytics>=8.1,<9.0

# Testing (unit + property-based).
pytest>=8.0,<9.0
hypothesis>=6.90,<7.0
