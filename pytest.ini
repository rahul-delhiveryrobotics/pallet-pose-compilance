[pytest]
testpaths = tests
pythonpath = src
addopts = -ra
# Property-based tests use Hypothesis (>= 100 iterations each; see design
# Testing Strategy). Tag format: "Feature: pallet-pose-compliance, Property {n}".
markers =
    property: property-based (Hypothesis) tests
    e2e: end-to-end pipeline tests
